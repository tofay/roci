cmd = ["curl", "--head", "https://google.com"]

[[entries]]
source = "/usr/bin/curl"
target = "/usr/bin/curl"

[[entries]]
source = "/etc/pki/tls/certs/ca-bundle.crt"
target = "/etc/pki/tls/certs/ca-bundle.crt"
mode = 0o644
